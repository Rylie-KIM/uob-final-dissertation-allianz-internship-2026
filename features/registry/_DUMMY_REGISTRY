Local stand-ins generated from src/data/real/ (itself dummy). NOT the real registry -
the real files come from features/extract_features.py (v2/v3) and
features/extract_features_v1.py (v1), run on the company laptop.
